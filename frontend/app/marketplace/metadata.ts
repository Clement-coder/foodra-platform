import { buildMetadata } from "@/lib/seo"

export const metadata = buildMetadata({
  title: "Marketplace",
  description: "Browse fresh agricultural products directly from verified Nigerian farmers. Buy grains, vegetables, fruits, livestock and more.",
  path: "/marketplace",
})

export { default } from "./page"
