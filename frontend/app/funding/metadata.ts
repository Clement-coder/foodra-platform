import { buildMetadata } from "@/lib/seo"

export const metadata = buildMetadata({
  title: "Funding",
  description: "Apply for agricultural loans and grants. Track your funding applications and get AI-powered credit scoring.",
  path: "/funding",
})
